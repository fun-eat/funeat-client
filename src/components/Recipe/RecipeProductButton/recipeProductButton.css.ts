import { style } from '@vanilla-extract/css';

export const container = style({
  width: 26,
  height: 26,
  background: '#D9D9D9',
  borderRadius: '50%',
});

export const translucent = style({
  opacity: '50%',
});
